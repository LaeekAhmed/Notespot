const express=require("express"),router=express.Router(),Book=require("../models/book");router.get("/",async(e,r)=>{let o;try{o=await Book.find().sort({createdAt:"desc"}).limit(10).exec()}catch{o=[]}r.render("index",{books:o})}),module.exports=router;